/* Exemplo de Programa na linguagem L */

int n<-1;
string nome;
const MAXITER=10;

write("Digite seu n@me: ");
readln(nome);

float percentual;
while n<=MAXITER {
  percentual<-float(n)/MAXITER;
  writeln(percentual,": Ola' ",nome);
  n<-n+1;
}
