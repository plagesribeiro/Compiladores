string nome;
int n;
n <- 3.14;
string nome;
const MAXITER=10;

write("Digite seu nome: ");
readln(nome);

float percentual;
while n<=MAXITER {
  percentual<-float(n)/MAXITER;
  writeln(percentual,": Ola' ",nome);
  n<-n+1;
}
