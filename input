int n <- 3;
int m <- 10;
while n > m {
  n <- n+1;
}


n <- 4.3;
string nome;
const MAXITER=10;

write("Digite seu nome: ");
readln(nome);

float percentual;
while n<=MAXITER {
  percentual<-float(n)/MAXITER;
  writeln(percentual,": Ola' ",nome);
  n<-n+1;
}
